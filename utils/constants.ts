// utils/constants.ts

// Re-export everything from the focused files for backward compatibility
export { sections, timelineEvents } from "./sections"
export { travelYears, locationData } from "./travel"
export { publications, projects, blogPosts } from "./content"
